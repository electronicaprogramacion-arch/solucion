@namespace CalibrationSaaS.Infraestructure.Blazor.Controls
@typeparam TableItem
@inherits Radzen.Blazor.RadzenDataGridColumn<TableItem>

@code {
    // This is just a placeholder component
    // The actual implementation is in RadzenGridColumn.cs
    // This component doesn't render anything on its own
}
